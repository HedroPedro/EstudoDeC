#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAXPATH 129

int main(){
    char filePath[MAXPATH], *content, tmpChar;
    FILE *currentFile;
    printf("Digite o nome do primeiro arquivo: ");
    setbuf(stdin, NULL);
}